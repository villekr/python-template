## Summary

<!-- What does this PR change and why? -->

## Changes

<!-- Bullet the notable changes. -->
-

## Verification

<!-- How did you verify this? Check what applies. -->
- [ ] `uv run ruff format --check src tests` and `uv run ruff check src tests` pass
- [ ] `uv run ty check` passes
- [ ] `uv run pytest` passes (coverage gate met)
- [ ] `uv run pip-audit` reports no new vulnerabilities

## Notes

<!-- Anything reviewers should know: tradeoffs, follow-ups, blocked items. -->
